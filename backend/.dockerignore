__pycache__
*.py[cod]
*.egg-info
.venv
venv
.env
.pytest_cache
.mypy_cache
htmlcov
.coverage
tests/
# alembic/versions/ is intentionally NOT excluded — migration files must
# be present inside the container so `alembic upgrade head` can run.
