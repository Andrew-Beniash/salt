"""Pydantic request / response schemas.

Schemas are organised per feature module (e.g. ``schemas/engagements.py``).
They are separate from ORM models so that API contracts can evolve
independently of the database schema.
"""
